/**
 * Non-intro motion timings — keep in sync with globals.css `@theme` reveal tokens.
 */
export const MOTION_REVEAL_IN_MS = 1100;
export const MOTION_REVEAL_OUT_MS = 760;
export const MOTION_HOME_RETURN_MS = 1600;

/** Grid ↔ project detail — keep in sync with globals.css page-transition tokens. */
export const MOTION_PAGE_OUT_MS = 900;
export const MOTION_PAGE_WHITE_HOLD_MS = 220;
